# Jetson Quick Link Test Placeholder

On the Jetson runtime project, the expected local commands are:

```bash
cd "/home/jetson/on-board computer"
bash scripts/quick_link_start.sh
bash scripts/quick_link_watch.sh
```

Only add these scripts here when the actual Jetson implementation is merged.
