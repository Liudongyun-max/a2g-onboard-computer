# Windows Ground Console

Current repository state includes the quick-link PowerShell test script. The full Windows Ground Station Console can be added under `windows/scripts/` without touching Jetson source files.

Windows must not directly modify Jetson source directories or send flight-control commands.
